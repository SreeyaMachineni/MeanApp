import { StatusCodeDirective } from './status-code.directive';

describe('StatusCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusCodeDirective();
    expect(directive).toBeTruthy();
  });
});
